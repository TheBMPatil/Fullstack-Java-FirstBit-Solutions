#include <stdio.h>
void main()
{
    // int a = 10, b = 20;
    // int c = a++ || b++;
    // printf("%d, %d, %d", a,b,c);
    // OP: 11, 20, 1

    // int a = 0, b = 20;
    // int c = a++ || b++;
    // printf("%d, %d, %d", a, b, c);
    // OP : 1, 21, 1

    // int a = 0, b = 0;
    // int c = a++ || b++;
    // printf("%d, %d, %d", a, b, c);
    // // OP:1, 1, 0

    // int a = 9, b = 8;
    // int c = printf("%d", ++a) + printf("%d", b++);
    // int d = printf("%d is the answer", c);
    // printf("%d", d);
    // // OP:1083 is the answer15

    // int a = 10, b = 20, c = 30;
    // int d = a++ + ++a + --a + b++;
    // printf("a=%d, b=%d, c=%d, d=%d", a, b, c, d);
    // // OP:a=11, b=21, c=30, d=53 //Compiler dependant

    // char ch = 'a';
    // int x = ch;
    // printf("%c = %d", ch, x); // a = 97
}